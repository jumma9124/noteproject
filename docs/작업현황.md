# 📋 작업 현황 (2026-02-26 기준)

> Windows PC ↔ Mac 왔다갔다 작업 중. **작업할 때마다 이 파일 업데이트 + push 필수!**

---

## 🖥️ 로컬 환경 시작

```bash
cd ~/Desktop/cursor/note-project   # (Mac 경로 확인 필요)
npm start                           # 개발 서버 실행 (http://localhost:3000)
```

> **주의**: 현재 `DEV_SKIP_AUTH = true` 설정으로 Google 로그인 없이 바로 진입됩니다. (배포 전 false로 변경 필요)

---

## ✅ 완료된 작업

### 인프라 / 공통
- [x] Firebase 연결 (firebase.js, auth.js)
- [x] React Router + 페이지 쉘 구성 (다이어리, 돈관리, 설정)
- [x] Tailwind CSS v3 설치
- [x] Google 로그인 화면 구현
- [x] Navbar + Layout 공통 컴포넌트

### 다이어리 (`src/features/diary/`)
- [x] **메인 달력** (`DiaryMain.jsx`) — 월별 달력, 휴가/일기 표시
- [x] **날짜 상세** (`DayPage.jsx`) — 휴가 입력, 일기 작성
- [x] **할일 페이지** (`TodosPage.jsx`) — 할일 추가/완료/삭제, D-day
- [x] **연간 목표 페이지** (`YearGoalsPage.jsx`) — 연간/월별 목표 작성

### 돈관리 (`src/features/finance/pages/FinanceMain.jsx`)
> 현재 모든 데이터는 **Mock 데이터** (Firestore 미연동)

- [x] **대시보드** 탭 — 총자산/부채/순자산, 파이차트, 목표 진행률, 주별 예산, 월별 지출 추이
- [x] **자산 관리** 탭 (6개 섹션)
  - [x] 주식 — 종목 목록, 매수 내역, 평단가/수익률 자동 계산, 현재가 수동 입력
  - [x] 적금 — 목록, 추가/수정, **월별 납입 체크박스** (1~12월)
  - [x] 채권 — 목록, 추가/수정, 만기금액 계산
  - [x] 펀드 — 목록, 추가/수정, 수익률 계산
  - [x] 예금/현금 — 목록, 추가/수정
  - [x] 부채 — 목록, 추가/수정
- [x] **지출 입력** 탭 — 주간 지출 목록, 날짜/메모/금액/**결제수단** 입력
- [x] **고정비** 탭 — 목록, 결제수단 포함 추가/수정
- [x] **통계** 탭 — 순자산 추이 선그래프, 월별 저축률 막대그래프
- [x] **목표** 탭 — 연간 목표 설정, 2컬럼 레이아웃 (차트 + 월별 테이블), 적금 납입 체크 → 달성액 자동 연동

### 핵심 기능 연동
- [x] **적금 납입 체크 → 목표 달성액 자동 반영**
  - 적금 탭에서 월별 납입 체크 시, 목표 탭의 해당 월 달성액에 자동 합산
  - `savings` state를 `FinanceMain`에서 관리해 두 탭이 공유

---

## 🔧 진행 중 / 남은 작업

### 우선순위 높음

#### 1. 설정 페이지 — 결제수단 관리
- **파일**: `src/features/settings/Settings.jsx`
- **현재 상태**: `<div>설정</div>` 껍데기만 있음
- **할 일**:
  - 고정비/지출에서 사용하는 결제수단 목록을 설정에서 편집 가능하게
  - 현재 `FinanceMain.jsx` 110번째 줄에 `METHODS = ['자동이체', '카드A', '카드B', '카드C', '현금']` 하드코딩
  - Settings에서 카드명 추가/삭제 → FinanceMain에서 동적으로 사용
  - (나중에) Firestore `users/{userId}/settings/paymentMethods`에 저장

#### 2. Firebase Firestore 연동
- **현재 상태**: 모든 데이터가 React state의 Mock 데이터
- **연동 필요 데이터**:
  - `finance/stocks` — 주식 종목 & 매수 내역
  - `finance/savings` — 적금 목록 & 납입 체크
  - `finance/bonds` — 채권
  - `finance/funds` — 펀드
  - `finance/deposits` — 예금/현금
  - `finance/debts` — 부채
  - `finance/weeklyExpenses` — 주별 지출 내역
  - `finance/fixedExpenses` — 고정비
  - `settings/yearGoal` — 연간 저축 목표 & 월별 목표
  - `settings/paymentMethods` — 결제수단 목록
- **참고**: `docs/DB구조_기술스택.md` — Firestore 상세 구조 정의됨

### 우선순위 중간

#### 3. 다이어리 Firestore 연동
- `calendar/{date}` — 휴가/일기
- `todos/{todoId}` — 할일
- `yearGoals/{year}` — 연간/월별 목표

#### 4. 통계 탭 실제 데이터 연동
- 현재 `MOCK_MONTHLY_TREND`, `MOCK_ASSET_TREND`, `MOCK_SAVINGS_RATE` 하드코딩
- Firestore 데이터 기반으로 자동 집계되도록

### 우선순위 낮음 (배포 직전)

#### 5. DEV_SKIP_AUTH 해제
- **파일**: `src/App.js` 15번째 줄
- `const DEV_SKIP_AUTH = true` → `false`
- Google 로그인 정상 동작 확인

---

## 📁 주요 파일 구조

```
src/
├─ App.js                          ← DEV_SKIP_AUTH 있음 (15번 줄)
├─ services/
│   ├─ firebase.js                 ← Firebase 초기화
│   └─ auth.js                     ← Google 로그인 함수
├─ features/
│   ├─ auth/Login.jsx              ← 로그인 화면
│   ├─ diary/pages/
│   │   ├─ DiaryMain.jsx           ← 달력 메인
│   │   ├─ DayPage.jsx             ← 날짜 상세
│   │   ├─ TodosPage.jsx           ← 할일 목록
│   │   └─ YearGoalsPage.jsx       ← 연간 목표
│   ├─ finance/pages/
│   │   └─ FinanceMain.jsx         ← 돈관리 전체 (단일 파일, ~1000줄)
│   └─ settings/
│       └─ Settings.jsx            ← 설정 (미구현)
└─ common/
    ├─ components/Navbar.jsx
    ├─ components/Layout.jsx
    └─ utils/dateUtils.js
```

---

## 🗄️ Firestore DB 구조 (설계)

```
users/{userId}/
  ├─ settings/
  │   ├─ totalVacation: 15
  │   ├─ yearlyGoal: 12000000
  │   ├─ monthlyGoals/{ 2026-01: 1000000, ... }
  │   └─ paymentMethods: ['자동이체', '카드A', ...]   ← 추가 예정
  ├─ calendar/{date}/  { vacation, diary, createdAt }
  ├─ todos/{todoId}/   { title, deadline, category, urgent, completed }
  ├─ yearGoals/{year}/ { yearGoal, monthGoals/{month} }
  └─ finance/
      ├─ assets/stock, savings, bonds, funds, cash
      ├─ debts/
      ├─ fixedExpenses/
      └─ weeklyExpenses/
```

---

## 🔑 기술 스택

| 항목 | 내용 |
|------|------|
| Framework | React 19 (Create React App) |
| Styling | Tailwind CSS v3 |
| Router | React Router DOM v7 |
| 차트 | Recharts |
| Backend | Firebase (Firestore + Auth + Hosting) |
| 배포 | Firebase Hosting (예정) |

---

## 💡 코딩 컨벤션

- 돈관리 전체가 **단일 파일** `FinanceMain.jsx`에 있음 (컴포넌트 분리 안 함)
- Mock 데이터는 파일 최상단에 `MOCK_XXX_INIT` 형태로 선언
- 공통 인풋 스타일: `const inp = 'border border-gray-200 rounded-lg px-3 py-2 text-sm ...'`
- 금액 포맷: `fmt(n)` = `n만원`, `fmtN(n)` = `n.toLocaleString()원`
- PC 전용, 모바일 레이아웃 고려 안 함

---

## ⚡ 다음 작업 추천 순서

1. **설정 페이지 결제수단 관리** — Settings.jsx 구현, FinanceMain과 연동
2. **Firestore 연동 (돈관리부터)** — 지출/고정비/적금 순서로
3. **Firestore 연동 (다이어리)** — 달력/할일 순서로
4. **배포** — DEV_SKIP_AUTH=false, Firebase Hosting 배포
